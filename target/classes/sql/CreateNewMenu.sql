CREATE TABLE JITAKU_MENU(ITEM_ID INT IDENTITY(1,1) PRIMARY KEY,
						 NAME VARCHAR(255) NOT NULL,
						 PRICE DECIMAL(10,3) NOT NULL,
						 DESCRIPTION VARCHAR(255),
						 CATEGORY VARCHAR(255),
						 TAGS VARCHAR(255));
